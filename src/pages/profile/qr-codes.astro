---
import BaseLayout from "../../layouts/BaseLayout.astro";
import VxHeader from "../../components/VxHeader.astro";
import VxFooter from "../../components/VxFooter.astro";
import PageWidgets from "../../components/PageWidgets.astro";
import { ROUTES } from "../../data/routes";

const title = "Your QR Codes — VOLYNX";
const description = "Manage your dynamic QR codes: edit destinations, pause, renew, view scans.";
---

<BaseLayout title={title} description={description}>
  <VxHeader primaryCtaLabel="Sign out" primaryCtaI18n="header.signout" primaryCtaHref="/login/?logout=1" primaryCtaAuth="in" />
  <PageWidgets hideUtilBar />

  <main class="qrp-shell">
    <div class="qrp-container">

      <header class="qrp-head">
        <div>
          <span class="qrp-kicker" data-i18n="qrp.kicker">QR Manager</span>
          <h1 data-i18n="qrp.title">Your dynamic QR Codes</h1>
          <p class="qrp-sub" data-i18n="qrp.subtitle">Edit destinations anytime, pause when needed, see live scan counts.</p>
        </div>
        <a href={ROUTES.qrgen} class="qrp-cta" data-i18n="qrp.create_new">+ Create new</a>
      </header>

      <section id="qrpLoading" class="qrp-state">
        <div class="qrp-spinner"></div>
        <p data-i18n="qrp.loading">Loading your QR codes…</p>
      </section>

      <section id="qrpLoggedOut" class="qrp-state" hidden>
        <h2 data-i18n="qrp.login_required">Sign in required</h2>
        <p data-i18n="qrp.login_msg">You need to sign in to view and manage your QR codes.</p>
        <a class="qrp-cta" href={`${ROUTES.login}?next=${ROUTES.profileQrCodes}`} data-i18n="qrp.btn_signin">Sign in</a>
      </section>

      <section id="qrpEmpty" class="qrp-state" hidden>
        <div class="qrp-empty-icon">📭</div>
        <h2 data-i18n="qrp.empty_title">No dynamic QRs yet</h2>
        <p data-i18n="qrp.empty_msg">Create your first dynamic QR code in QRGen. You can change its destination URL anytime.</p>
        <a class="qrp-cta" href={ROUTES.qrgen} data-i18n="qrp.btn_create_first">Create my first QR</a>
      </section>

      <section id="qrpList" hidden>
        <div class="qrp-quota-bar">
          <span class="qrp-quota-text" id="qrpQuotaText">— / —</span>
          <a href={ROUTES.pricing} class="qrp-quota-upgrade" data-i18n="qrp.upgrade">Upgrade plan →</a>
        </div>

        <div id="qrpItems" class="qrp-items"></div>
      </section>

      <!-- Edit modal -->
      <div id="qrpEditModal" class="qrp-modal" hidden>
        <div class="qrp-modal-backdrop" data-close></div>
        <div class="qrp-modal-card">
          <h3 data-i18n="qrp.edit_title">Edit QR destination</h3>
          <p class="qrp-modal-desc" data-i18n="qrp.edit_desc">The short URL stays the same. Only the destination changes.</p>
          <label data-i18n="qrp.edit_target_label">New destination URL</label>
          <input type="url" id="qrpEditTarget" placeholder="https://exemplo.com" autocomplete="url" />
          <label data-i18n="qrp.edit_label_label">Label (optional)</label>
          <input type="text" id="qrpEditLabel" maxlength="100" />
          <div id="qrpEditError" class="qrp-modal-error" hidden></div>
          <div class="qrp-modal-actions">
            <button type="button" class="qrp-btn ghost" data-close data-i18n="qrp.btn_cancel">Cancel</button>
            <button type="button" id="qrpEditSave" class="qrp-btn primary" data-i18n="qrp.btn_save">Save</button>
          </div>
        </div>
      </div>

      <!-- Transfer modal -->
      <div id="qrpTransferModal" class="qrp-modal" hidden>
        <div class="qrp-modal-backdrop" data-transfer-close></div>
        <div class="qrp-modal-card">
          <h3>Transfer QR ownership</h3>
          <p class="qrp-modal-desc">The short URL stays the same. The recipient must have compatible QR capacity to manage this QR from their profile.</p>
          <label>Recipient account email</label>
          <input type="email" id="qrpTransferEmail" placeholder="client@example.com" autocomplete="email" />
          <div class="qrp-gift-note">
            If the recipient cannot receive this QR yet, you can gift a compatible subscription or ask them to upgrade before transferring.
          </div>
          <div id="qrpTransferError" class="qrp-modal-error" hidden></div>
          <div class="qrp-modal-actions">
            <button type="button" class="qrp-btn ghost" data-transfer-close>Cancel</button>
            <a class="qrp-btn ghost qrp-btn-link" href={ROUTES.pricing}>Gift subscription</a>
            <button type="button" id="qrpTransferSave" class="qrp-btn primary">Transfer</button>
          </div>
        </div>
      </div>

    </div>
  </main>

  <VxFooter />

  <style>
    body { background: #0a0a0a; color: #e8e8e8; }

    .qrp-shell {
      min-height: calc(100vh - 200px);
      padding: 100px 20px 80px;
      position: relative;
    }
    .qrp-container { max-width: 980px; margin: 0 auto; }

    .qrp-head {
      display: flex;
      align-items: flex-end;
      justify-content: space-between;
      gap: 24px;
      margin-bottom: 36px;
      flex-wrap: wrap;
    }
    .qrp-kicker {
      display: inline-block;
      font-size: .72rem;
      font-weight: 600;
      letter-spacing: 1.5px;
      text-transform: uppercase;
      color: rgba(89,198,232,.85);
      margin-bottom: 8px;
    }
    .qrp-head h1 {
      font-size: clamp(1.6rem, 3.2vw, 2.2rem);
      font-weight: 700;
      letter-spacing: -.01em;
      margin-bottom: 8px;
    }
    .qrp-sub {
      color: rgba(255,255,255,.6);
      max-width: 520px;
      line-height: 1.5;
    }

    .qrp-cta {
      display: inline-flex;
      align-items: center;
      padding: 11px 20px;
      background: linear-gradient(135deg, #6c0ae4, #210df7);
      color: #fff;
      text-decoration: none;
      border-radius: 12px;
      font-weight: 600;
      font-size: .92rem;
      box-shadow: 0 6px 18px rgba(108,10,228,.3);
      transition: transform .15s ease;
    }
    .qrp-cta:hover { transform: translateY(-1px); }

    .qrp-state {
      text-align: center;
      padding: 64px 24px;
      background: rgba(255,255,255,.03);
      border: 1px solid rgba(255,255,255,.06);
      border-radius: 18px;
    }
    .qrp-state h2 { font-size: 1.3rem; margin-bottom: 10px; }
    .qrp-state p { color: rgba(255,255,255,.6); margin-bottom: 18px; max-width: 420px; margin-inline: auto; line-height: 1.5; }
    .qrp-empty-icon { font-size: 48px; margin-bottom: 18px; opacity: .85; }

    .qrp-spinner {
      width: 32px; height: 32px;
      border: 3px solid rgba(255,255,255,.12);
      border-top-color: rgba(89,198,232,.7);
      border-radius: 50%;
      margin: 0 auto 18px;
      animation: qrp-spin .8s linear infinite;
    }
    @keyframes qrp-spin { to { transform: rotate(360deg); } }

    .qrp-quota-bar {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 12px 18px;
      background: rgba(255,255,255,.03);
      border: 1px solid rgba(255,255,255,.06);
      border-radius: 12px;
      margin-bottom: 24px;
      font-size: .88rem;
    }
    .qrp-quota-text { color: rgba(255,255,255,.75); }
    .qrp-quota-upgrade {
      color: rgba(89,198,232,.85);
      text-decoration: none;
      font-weight: 500;
      font-size: .82rem;
    }
    .qrp-quota-upgrade:hover { text-decoration: underline; }

    .qrp-items { display: flex; flex-direction: column; gap: 12px; }

    .qrp-item {
      display: grid;
      grid-template-columns: auto 1fr auto;
      gap: 16px;
      padding: 20px;
      background:
        radial-gradient(circle at 0% 0%, rgba(89,198,232,.12), transparent 36%),
        radial-gradient(circle at 100% 100%, rgba(198,255,143,.06), transparent 34%),
        rgba(255,255,255,.035);
      border: 1px solid rgba(89,198,232,.12);
      border-radius: 14px;
      align-items: center;
      box-shadow: inset 0 0 22px rgba(255,255,255,.025), 0 12px 34px rgba(0,0,0,.16);
      transition: border-color .15s, transform .15s, box-shadow .15s;
    }
    .qrp-item:hover {
      border-color: rgba(89,198,232,.32);
      transform: translateY(-1px);
      box-shadow: inset 0 0 24px rgba(89,198,232,.04), 0 16px 42px rgba(0,0,0,.22);
    }
    .qrp-item--paused { opacity: .65; }
    .qrp-item--grace { border-color: rgba(255,200,80,.3); }
    .qrp-item--expired,
    .qrp-item--admin_blocked { opacity: .55; }

    .qrp-status {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      justify-content: center;
      min-width: 108px;
      padding: 9px 12px;
      border-radius: 10px;
      font-size: .68rem;
      font-weight: 900;
      letter-spacing: .4px;
      text-transform: uppercase;
    }
    .qrp-status--active {
      background: rgba(0,64,26,.26);
      color: #02dc02;
      border: 1px outset rgba(0,255,0,.72);
      text-shadow: 0 0 10px #71ff71;
      box-shadow: inset 0 0 10px rgba(3,255,3,.36), 0 0 10px rgba(147,255,147,.28);
    }
    .qrp-status--paused { background: rgba(255,255,255,.08); color: rgba(255,255,255,.6); }
    .qrp-status--grace { background: rgba(255,200,80,.12); color: #fbbf24; }
    .qrp-status--expired,
    .qrp-status--admin_blocked { background: rgba(248,113,113,.12); color: #f87171; }

    .qrp-item-body { min-width: 0; }
    .qrp-item-title {
      font-size: 1.08rem;
      font-weight: 750;
      margin-bottom: 4px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      color: rgba(255,255,255,.94);
    }
    .qrp-item-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      font-size: .78rem;
      color: rgba(255,255,255,.55);
    }
    .qrp-item-meta .copy-btn {
      background: rgba(89,198,232,.09);
      border: 1px solid rgba(89,198,232,.16);
      color: rgba(218,250,255,.8);
      cursor: pointer;
      padding: 7px 10px;
      font-size: inherit;
      font-family: 'SF Mono', Menlo, monospace;
      border-radius: 10px;
      box-shadow: inset 0 0 12px rgba(89,198,232,.05);
      transition: background .15s, color .15s, border-color .15s, box-shadow .15s;
    }
    .qrp-item-meta .copy-btn:hover {
      background: rgba(89,198,232,.14);
      color: #fff;
      border-color: rgba(89,198,232,.32);
      box-shadow: 0 0 14px rgba(89,198,232,.12);
    }
    .qrp-target {
      max-width: 320px;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      display: inline-block;
      vertical-align: middle;
    }

    .qrp-actions {
      display: flex;
      gap: 8px;
      align-items: center;
    }
    .qrp-action {
      width: 40px;
      height: 40px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      background:
        linear-gradient(135deg, rgba(89,198,232,.15), rgba(198,255,143,.045)),
        rgba(255,255,255,.035);
      border: 1px solid rgba(89,198,232,.16);
      border-radius: 8px;
      color: rgba(235,253,255,.88);
      cursor: pointer;
      box-shadow: inset 0 0 16px rgba(89,198,232,.055), 0 0 14px rgba(89,198,232,.055);
      transition: all .15s;
      font-size: .85rem;
    }
    .qrp-action:hover {
      background:
        linear-gradient(135deg, rgba(89,198,232,.24), rgba(198,255,143,.08)),
        rgba(255,255,255,.045);
      color: #fff;
      border-color: rgba(89,198,232,.38);
      transform: translateY(-1px);
      box-shadow: inset 0 0 16px rgba(89,198,232,.08), 0 0 18px rgba(89,198,232,.16);
    }
    .qrp-action--danger:hover {
      background: rgba(248,113,113,.15);
      border-color: rgba(248,113,113,.4);
      color: #fca5a5;
    }

    :global(.qrp-items .qrp-item) {
      display: grid;
      grid-template-columns: auto 1fr auto;
      gap: 16px;
      padding: 20px;
      background:
        radial-gradient(circle at 0% 0%, rgba(89,198,232,.12), transparent 36%),
        radial-gradient(circle at 100% 100%, rgba(198,255,143,.06), transparent 34%),
        rgba(255,255,255,.035);
      border: 1px solid rgba(89,198,232,.12);
      border-radius: 14px;
      align-items: center;
      box-shadow: inset 0 0 22px rgba(255,255,255,.025), 0 12px 34px rgba(0,0,0,.16);
      transition: border-color .15s, transform .15s, box-shadow .15s;
    }

    :global(.qrp-items .qrp-item:hover) {
      border-color: rgba(89,198,232,.32);
      transform: translateY(-1px);
      box-shadow: inset 0 0 24px rgba(89,198,232,.04), 0 16px 42px rgba(0,0,0,.22);
    }

    :global(.qrp-items .qrp-status) {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-width: 108px;
      padding: 9px 12px;
      border-radius: 10px;
      font-size: .68rem;
      font-weight: 900;
      letter-spacing: .4px;
      text-transform: uppercase;
    }

    :global(.qrp-items .qrp-status--active) {
      background: rgba(0,64,26,.26);
      color: #02dc02;
      border: 1px outset rgba(0,255,0,.72);
      text-shadow: 0 0 10px #71ff71;
      box-shadow: inset 0 0 10px rgba(3,255,3,.36), 0 0 10px rgba(147,255,147,.28);
    }

    :global(.qrp-items .qrp-status--paused) { background: rgba(255,255,255,.08); color: rgba(255,255,255,.6); }
    :global(.qrp-items .qrp-status--grace) { background: rgba(255,200,80,.12); color: #fbbf24; }
    :global(.qrp-items .qrp-status--expired),
    :global(.qrp-items .qrp-status--admin_blocked) { background: rgba(248,113,113,.12); color: #f87171; }

    :global(.qrp-items .qrp-item-title) {
      font-size: 1.08rem;
      font-weight: 750;
      margin-bottom: 4px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      color: rgba(255,255,255,.94);
    }

    :global(.qrp-items .qrp-item-meta) {
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      font-size: .78rem;
      color: rgba(255,255,255,.55);
    }

    :global(.qrp-items .qrp-item-meta .copy-btn) {
      background: rgba(89,198,232,.09);
      border: 1px solid rgba(89,198,232,.16);
      color: rgba(218,250,255,.8);
      cursor: pointer;
      padding: 7px 10px;
      font-size: inherit;
      font-family: 'SF Mono', Menlo, monospace;
      border-radius: 10px;
      box-shadow: inset 0 0 12px rgba(89,198,232,.05);
      transition: background .15s, color .15s, border-color .15s, box-shadow .15s;
    }

    :global(.qrp-items .qrp-item-meta .copy-btn:hover) {
      background: rgba(89,198,232,.14);
      color: #fff;
      border-color: rgba(89,198,232,.32);
      box-shadow: 0 0 14px rgba(89,198,232,.12);
    }

    :global(.qrp-items .qrp-actions) {
      display: flex;
      gap: 8px;
      align-items: center;
    }

    :global(.qrp-items .qrp-action) {
      width: 40px;
      height: 40px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      background:
        linear-gradient(135deg, rgba(89,198,232,.15), rgba(198,255,143,.045)),
        rgba(255,255,255,.035);
      border: 1px solid rgba(89,198,232,.16);
      border-radius: 8px;
      color: rgba(235,253,255,.88);
      cursor: pointer;
      box-shadow: inset 0 0 16px rgba(89,198,232,.055), 0 0 14px rgba(89,198,232,.055);
      transition: all .15s;
      font-size: .85rem;
    }

    :global(.qrp-items .qrp-action:hover) {
      background:
        linear-gradient(135deg, rgba(89,198,232,.24), rgba(198,255,143,.08)),
        rgba(255,255,255,.045);
      color: #fff;
      border-color: rgba(89,198,232,.38);
      transform: translateY(-1px);
      box-shadow: inset 0 0 16px rgba(89,198,232,.08), 0 0 18px rgba(89,198,232,.16);
    }

    :global(.qrp-items .qrp-action--danger:hover) {
      background: rgba(248,113,113,.15);
      border-color: rgba(248,113,113,.4);
      color: #fca5a5;
    }

    /* Modal */
    .qrp-modal {
      position: fixed;
      inset: 0;
      z-index: 9999;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }
    .qrp-modal[hidden] {
      display: none !important;
    }
    .qrp-modal-backdrop {
      position: absolute;
      inset: 0;
      background: rgba(0,0,0,.7);
      backdrop-filter: blur(8px);
    }
    .qrp-modal-card {
      position: relative;
      background: #1a1a2e;
      border: 1px solid rgba(255,255,255,.1);
      border-radius: 16px;
      padding: 28px;
      max-width: 460px;
      width: 100%;
    }
    .qrp-modal-card h3 { font-size: 1.15rem; margin-bottom: 6px; }
    .qrp-modal-desc { color: rgba(255,255,255,.6); font-size: .85rem; margin-bottom: 18px; }
    .qrp-modal-card label {
      display: block;
      font-size: .78rem;
      color: rgba(255,255,255,.55);
      margin-bottom: 6px;
      margin-top: 14px;
    }
    .qrp-modal-card input {
      width: 100%;
      padding: 11px 14px;
      background: rgba(255,255,255,.05);
      border: 1px solid rgba(255,255,255,.1);
      border-radius: 8px;
      color: #fff;
      font-size: .92rem;
      outline: none;
    }
    .qrp-modal-card input:focus { border-color: rgba(89,198,232,.4); }
    .qrp-modal-error {
      margin-top: 14px;
      padding: 10px 12px;
      background: rgba(248,113,113,.1);
      border: 1px solid rgba(248,113,113,.25);
      border-radius: 8px;
      font-size: .85rem;
      color: #fca5a5;
    }
    .qrp-gift-note {
      margin-top: 14px;
      padding: 10px 12px;
      border-radius: 8px;
      border: 1px solid rgba(245,196,81,.22);
      background: rgba(245,196,81,.08);
      color: rgba(255,255,255,.7);
      font-size: .82rem;
      line-height: 1.5;
    }
    .qrp-modal-actions {
      display: flex;
      justify-content: flex-end;
      gap: 10px;
      margin-top: 22px;
    }
    .qrp-btn {
      padding: 10px 20px;
      border-radius: 10px;
      border: 1px solid transparent;
      cursor: pointer;
      font-size: .88rem;
      font-weight: 500;
      transition: all .15s;
    }
    .qrp-btn.ghost {
      background: transparent;
      color: rgba(255,255,255,.6);
      border-color: rgba(255,255,255,.12);
    }
    .qrp-btn.ghost:hover { color: #fff; border-color: rgba(255,255,255,.2); }
    .qrp-btn.primary {
      background: linear-gradient(135deg, #6c0ae4, #210df7);
      color: #fff;
    }
    .qrp-btn-link { text-decoration: none; }
    .qrp-btn.primary:hover { transform: translateY(-1px); }
    .qrp-btn:disabled { opacity: .5; cursor: not-allowed; }

    @media (max-width: 640px) {
      .qrp-item {
        grid-template-columns: 1fr;
        gap: 10px;
      }
      .qrp-actions { flex-wrap: wrap; }
      .qrp-target { max-width: 100%; }
    }
  </style>

  <script is:inline src="/js/vx-auth-bridge.js"></script>
  <script is:inline src="/js/vx-token-refresh.js?v=20260515-qr-profile-auth"></script>
  <script is:inline src="/scripts/qr-profile.js?v=20260604-transfer-owner"></script>
</BaseLayout>
